{
  "name": "talk-webcomponents",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --directory --files src/**/* --serveStatic src",
    "serve": "ws --port 8080  --compress --directory src",
    "talk:dev": "browser-sync start --server --directory --files folien/**/* --serveStatic folien",
    "talk": "ws --port 8081  --compress --directory folien"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.26.7",
    "local-web-server": "^3.0.2"
  }
}
